<h2>Categories list:</h2>
<hr>
<a href="/admin/categories/add">
	<button class="btn btn-success">New category</button>
</a>

{{#each categories}}
	<div class="card mt-4">
		<div class="card-body">
			<h4>{{showName this}}</h4>
			<small>{{showSlug this}}</small>
			<br>
			<a href="/admin/categories/edit/{{showId this}}">
				<button class="btn btn-success mt-2">Edit</button>
			</a>
			<form action="/admin/categories/delete" method="POST">
				<input type="hidden" name="id" value="{{showId this}}">
				<button type="submit" class="btn btn-danger mt-2">Delete</button>
			</form>
		</div>
	</div>
{{else}}
	<h4>There aren't categories created yet.</h4>

{{/each}}